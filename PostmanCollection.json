{
  "info": {
    "_postman_id": "cb1e2f3a-4d5b-6c7e-8f9a-0b1c2d3e4f5a",
    "name": "CodeBloggs Module 10 Backend",
    "description": "## CodeBloggs Module 10 Backend Collection\n\nThis collection preserves the working Module 9 backend requests and adds the completed Module 10 backend requests. Requests are parameterized with Postman variables and are intended to run with the companion `CodeBloggs Module 10 Backend Environment.postman_environment.json` environment.\n\n### Local Setup\n1. Start MongoDB access and the backend server.\n2. Select the CodeBloggs Module 10 Backend Environment in Postman.\n3. Confirm `base_url` points to the backend in the selected environment.\n4. Run setup requests in order when testing happy paths: Create User, Login, Create Post, Create Comment, Create Reply.\n5. Use only throwaway records for destructive Module 10 delete requests.\n\n### Automatic Variable Chaining\n- Create User saves `user_id`.\n- Login saves `user_id`, `session_token`, and `session_id` when available from the response/cookie jar.\n- Create Post saves `post_id`.\n- Create Comment saves `comment_id`.\n- Create Reply saves `reply_id`.\n\n### Module 10 Requests\nThe `Module 10 Backend` folder contains happy-path, malformed-id, and nonexistent-id scenarios for all completed Module 10 backend endpoints.",
    "schema": "https://schema.getpostman.com/json/collection/v2.1.0/collection.json"
  },
  "variable": [
    {
      "key": "base_url",
      "value": "",
      "type": "string",
      "description": "Backend base URL."
    },
    {
      "key": "user_id",
      "value": "",
      "type": "string",
      "description": "MongoDB user _id captured by Create User or Login."
    },
    {
      "key": "post_id",
      "value": "",
      "type": "string",
      "description": "MongoDB post _id captured by Create Post."
    },
    {
      "key": "comment_id",
      "value": "",
      "type": "string",
      "description": "MongoDB comment _id captured by Create Comment."
    },
    {
      "key": "reply_id",
      "value": "",
      "type": "string",
      "description": "MongoDB reply _id captured by Create Reply."
    },
    {
      "key": "profile_picture_id",
      "value": "",
      "type": "string",
      "description": "Reserved for APIs that return a profile picture document id."
    },
    {
      "key": "session_id",
      "value": "",
      "type": "string",
      "description": "Session id captured from the session_token cookie after Login."
    },
    {
      "key": "session_token",
      "value": "",
      "type": "string",
      "description": "Cookie token captured after Login."
    },
    {
      "key": "jwt_token",
      "value": "",
      "type": "string",
      "description": "Reserved for JWT auth if the API adds it later."
    },
    {
      "key": "email",
      "value": "",
      "type": "string",
      "description": "Test user email. Use a unique value when rerunning registration."
    },
    {
      "key": "password",
      "value": "",
      "type": "string",
      "description": "Test user password."
    },
    {
      "key": "first_name",
      "value": "",
      "type": "string",
      "description": "Registration first name."
    },
    {
      "key": "last_name",
      "value": "",
      "type": "string",
      "description": "Registration last name."
    },
    {
      "key": "birthday",
      "value": "",
      "type": "string",
      "description": "Registration birthday."
    },
    {
      "key": "location",
      "value": "",
      "type": "string",
      "description": "Test user location."
    },
    {
      "key": "occupation",
      "value": "",
      "type": "string",
      "description": "Test user occupation."
    },
    {
      "key": "status",
      "value": "",
      "type": "string",
      "description": "Boolean user status value used in update requests."
    },
    {
      "key": "auth_level",
      "value": "",
      "type": "string",
      "description": "User auth level used when needed."
    },
    {
      "key": "updated_first_name",
      "value": "",
      "type": "string",
      "description": "User update first name."
    },
    {
      "key": "updated_last_name",
      "value": "",
      "type": "string",
      "description": "User update last name."
    },
    {
      "key": "updated_location",
      "value": "",
      "type": "string",
      "description": "User update location."
    },
    {
      "key": "updated_occupation",
      "value": "",
      "type": "string",
      "description": "User update occupation."
    },
    {
      "key": "post_content",
      "value": "",
      "type": "string",
      "description": "Create Post content."
    },
    {
      "key": "comment_content",
      "value": "",
      "type": "string",
      "description": "Create Comment content."
    },
    {
      "key": "updated_comment_content",
      "value": "",
      "type": "string",
      "description": "Update Comment content."
    },
    {
      "key": "reply_content",
      "value": "",
      "type": "string",
      "description": "Create Reply content."
    },
    {
      "key": "parent_type",
      "value": "",
      "type": "string",
      "description": "Reply parent type."
    },
    {
      "key": "reply_depth",
      "value": "",
      "type": "string",
      "description": "Reply nesting depth."
    },
    {
      "key": "likes_delta",
      "value": "",
      "type": "string",
      "description": "Amount to increment likes by."
    },
    {
      "key": "malformed_id",
      "value": "",
      "type": "string",
      "description": "Reusable malformed MongoDB id for error tests."
    },
    {
      "key": "nonexistent_user_id",
      "value": "",
      "type": "string",
      "description": "Valid ObjectId that should not match a user."
    },
    {
      "key": "nonexistent_post_id",
      "value": "",
      "type": "string",
      "description": "Valid ObjectId that should not match a post."
    },
    {
      "key": "nonexistent_comment_id",
      "value": "",
      "type": "string",
      "description": "Valid ObjectId that should not match a comment."
    },
    {
      "key": "page",
      "value": "",
      "type": "string",
      "description": "Reusable page query parameter."
    },
    {
      "key": "limit",
      "value": "",
      "type": "string",
      "description": "Reusable limit query parameter."
    },
    {
      "key": "sort",
      "value": "",
      "type": "string",
      "description": "Reusable sort query parameter."
    }
  ],
  "item": [
    {
      "name": "User",
      "description": "Module 9 user registration and retrieval requests. These are preserved for setup and regression testing.",
      "item": [
        {
          "name": "Create User",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{base_url}}/user",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "user"
              ]
            },
            "description": "Endpoint: POST /user\n\nSetup instructions:\nNo prior request required. Use a unique email each time if the database already has this user.\n\nRequired variables:\n- base_url\n\nRequired headers:\nContent-Type: application/json\n\nRequired body:\nJSON user registration fields: first_name, last_name, email, password, birthday, optional location, optional occupation.\n\nExpected success response:\n{ status: \"ok\", data: { user }, message: \"Registration successful. Please log in.\" }\n\nExpected error responses:\n400 validation/missing fields, 409 duplicate email, 500 unexpected server error.",
            "body": {
              "mode": "raw",
              "raw": "{\n  \"first_name\": \"{{first_name}}\",\n  \"last_name\": \"{{last_name}}\",\n  \"email\": \"{{email}}\",\n  \"password\": \"{{password}}\",\n  \"birthday\": \"{{birthday}}\",\n  \"location\": \"{{location}}\",\n  \"occupation\": \"{{occupation}}\"\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('Status code is 201', () => pm.response.to.have.status(201));",
                  "pm.test('Response time is acceptable', () => pm.expect(pm.response.responseTime).to.be.below(2000));",
                  "pm.test('Response is JSON', () => pm.response.to.be.json);",
                  "pm.test('Content-Type is JSON', () => pm.expect(pm.response.headers.get('Content-Type') || '').to.include('application/json'));",
                  "pm.test('Response envelope is valid', () => {",
                  "  const body = pm.response.json();",
                  "  pm.expect(body).to.have.property('status');",
                  "  pm.expect(body).to.have.property('data');",
                  "  pm.expect(body).to.have.property('message');",
                  "});",
                  "pm.test('Captures user_id and omits password', () => {",
                  "  const body = pm.response.json();",
                  "  pm.expect(body.status).to.equal('ok');",
                  "  pm.expect(body.data.user).to.not.have.property('password');",
                  "  pm.expect(body.data.user._id).to.be.a('string');",
                  "  pm.environment.set('user_id', body.data.user._id);",
                  "});"
                ]
              }
            }
          ]
        },
        {
          "name": "Get All Users",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{base_url}}/user",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "user"
              ]
            },
            "description": "Endpoint: GET /user\n\nSetup instructions:\nOptional: run Create User first so at least one user exists.\n\nRequired variables:\n- base_url\n\nRequired headers:\nNone\n\nRequired body:\nNone\n\nExpected success response:\n{ status: \"ok\", data: { users: [] }, message: \"Users retrieved successfully\" }\n\nExpected error responses:\n500 unexpected server error."
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('Status code is 200', () => pm.response.to.have.status(200));",
                  "pm.test('Response time is acceptable', () => pm.expect(pm.response.responseTime).to.be.below(2000));",
                  "pm.test('Response is JSON', () => pm.response.to.be.json);",
                  "pm.test('Content-Type is JSON', () => pm.expect(pm.response.headers.get('Content-Type') || '').to.include('application/json'));",
                  "pm.test('Response envelope is valid', () => {",
                  "  const body = pm.response.json();",
                  "  pm.expect(body).to.have.property('status');",
                  "  pm.expect(body).to.have.property('data');",
                  "  pm.expect(body).to.have.property('message');",
                  "});",
                  "pm.test('Returns users array without passwords', () => {",
                  "  const body = pm.response.json();",
                  "  pm.expect(body.data.users).to.be.an('array');",
                  "  body.data.users.forEach(user => pm.expect(user).to.not.have.property('password'));",
                  "});"
                ]
              }
            }
          ]
        },
        {
          "name": "Get User by ID",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{base_url}}/user/{{user_id}}",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "user",
                "{{user_id}}"
              ]
            },
            "description": "Endpoint: GET /user/:id\n\nSetup instructions:\nRun Create User first or set user_id to an existing user.\n\nRequired variables:\n- base_url\n- user_id\n\nRequired headers:\nNone\n\nRequired body:\nNone\n\nExpected success response:\n{ status: \"ok\", data: { user }, message: \"User retrieved successfully\" }\n\nExpected error responses:\n404 user not found or malformed id, 500 unexpected server error."
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('Status code is 200', () => pm.response.to.have.status(200));",
                  "pm.test('Response time is acceptable', () => pm.expect(pm.response.responseTime).to.be.below(2000));",
                  "pm.test('Response is JSON', () => pm.response.to.be.json);",
                  "pm.test('Content-Type is JSON', () => pm.expect(pm.response.headers.get('Content-Type') || '').to.include('application/json'));",
                  "pm.test('Response envelope is valid', () => {",
                  "  const body = pm.response.json();",
                  "  pm.expect(body).to.have.property('status');",
                  "  pm.expect(body).to.have.property('data');",
                  "  pm.expect(body).to.have.property('message');",
                  "});",
                  "pm.test('Returns user without password', () => {",
                  "  const body = pm.response.json();",
                  "  pm.expect(body.data.user).to.not.have.property('password');",
                  "});"
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "Session",
      "description": "Module 9 cookie-based authentication requests. Login sets the session_token cookie used by protected create/upload routes.",
      "item": [
        {
          "name": "Login",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{base_url}}/session",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "session"
              ]
            },
            "description": "Endpoint: POST /session\n\nSetup instructions:\nRun Create User first or use credentials for an existing user.\n\nRequired variables:\n- base_url\n\nRequired headers:\nContent-Type: application/json\n\nRequired body:\nJSON with email and password.\n\nExpected success response:\n{ status: \"ok\", data: { user }, message: \"Login successful\" } and HTTP-only session_token cookie.\n\nExpected error responses:\n401 invalid email/password, 500 unexpected server error.",
            "body": {
              "mode": "raw",
              "raw": "{\n  \"email\": \"{{email}}\",\n  \"password\": \"{{password}}\"\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('Status code is 200', () => pm.response.to.have.status(200));",
                  "pm.test('Response time is acceptable', () => pm.expect(pm.response.responseTime).to.be.below(2000));",
                  "pm.test('Response is JSON', () => pm.response.to.be.json);",
                  "pm.test('Content-Type is JSON', () => pm.expect(pm.response.headers.get('Content-Type') || '').to.include('application/json'));",
                  "pm.test('Response envelope is valid', () => {",
                  "  const body = pm.response.json();",
                  "  pm.expect(body).to.have.property('status');",
                  "  pm.expect(body).to.have.property('data');",
                  "  pm.expect(body).to.have.property('message');",
                  "});",
                  "pm.test('Message is Login successful', () => {",
                  "  const body = pm.response.json();",
                  "  pm.expect(body.status).to.equal('ok');",
                  "  pm.expect(body.message).to.equal('Login successful');",
                  "  pm.expect(body.data.user).to.not.have.property('password');",
                  "  if (body.data.user && body.data.user._id) pm.environment.set('user_id', body.data.user._id);",
                  "});",
                  "pm.test('Captures session token cookie when available', () => {",
                  "  const token = pm.cookies.get('session_token');",
                  "  if (token) {",
                  "    pm.environment.set('session_token', token);",
                  "    pm.environment.set('session_id', token);",
                  "  }",
                  "  pm.expect(pm.cookies.has('session_token')).to.be.true;",
                  "});"
                ]
              }
            }
          ]
        },
        {
          "name": "Validate Session",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{base_url}}/session/validate",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "session",
                "validate"
              ]
            },
            "description": "Endpoint: GET /session/validate\n\nSetup instructions:\nRun Login first so Postman has a valid session_token cookie.\n\nRequired variables:\n- base_url\n\nRequired headers:\nCookie: session_token is handled by Postman after Login.\n\nRequired body:\nNone\n\nExpected success response:\n{ status: \"ok\", data: { user }, message: \"Session is valid\" }\n\nExpected error responses:\n401 unauthorized, 500 unexpected server error."
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('Status code is 200', () => pm.response.to.have.status(200));",
                  "pm.test('Response time is acceptable', () => pm.expect(pm.response.responseTime).to.be.below(2000));",
                  "pm.test('Response is JSON', () => pm.response.to.be.json);",
                  "pm.test('Content-Type is JSON', () => pm.expect(pm.response.headers.get('Content-Type') || '').to.include('application/json'));",
                  "pm.test('Response envelope is valid', () => {",
                  "  const body = pm.response.json();",
                  "  pm.expect(body).to.have.property('status');",
                  "  pm.expect(body).to.have.property('data');",
                  "  pm.expect(body).to.have.property('message');",
                  "});",
                  "pm.test('Message is Session is valid', () => {",
                  "  const body = pm.response.json();",
                  "  pm.expect(body.status).to.equal('ok');",
                  "  pm.expect(body.message).to.equal('Session is valid');",
                  "});"
                ]
              }
            }
          ]
        },
        {
          "name": "Logout",
          "request": {
            "method": "DELETE",
            "header": [],
            "url": {
              "raw": "{{base_url}}/session",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "session"
              ]
            },
            "description": "Endpoint: DELETE /session\n\nSetup instructions:\nOptional: run Login first. This endpoint is idempotent.\n\nRequired variables:\n- base_url\n\nRequired headers:\nCookie: session_token is handled by Postman after Login.\n\nRequired body:\nNone\n\nExpected success response:\n{ status: \"ok\", data: {}, message: \"Logged out successfully\" }\n\nExpected error responses:\n500 unexpected server error."
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('Status code is 200', () => pm.response.to.have.status(200));",
                  "pm.test('Response time is acceptable', () => pm.expect(pm.response.responseTime).to.be.below(2000));",
                  "pm.test('Response is JSON', () => pm.response.to.be.json);",
                  "pm.test('Content-Type is JSON', () => pm.expect(pm.response.headers.get('Content-Type') || '').to.include('application/json'));",
                  "pm.test('Response envelope is valid', () => {",
                  "  const body = pm.response.json();",
                  "  pm.expect(body).to.have.property('status');",
                  "  pm.expect(body).to.have.property('data');",
                  "  pm.expect(body).to.have.property('message');",
                  "});",
                  "pm.test('Message is Logged out successfully', () => {",
                  "  const body = pm.response.json();",
                  "  pm.expect(body.status).to.equal('ok');",
                  "  pm.expect(body.message).to.equal('Logged out successfully');",
                  "});"
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "Presence",
      "description": "Module 9 presence request. Requires a valid session cookie.",
      "item": [
        {
          "name": "Get Active Users",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{base_url}}/presence",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "presence"
              ]
            },
            "description": "Endpoint: GET /presence\n\nSetup instructions:\nRun Login first. This request also refreshes the caller presence heartbeat.\n\nRequired variables:\n- base_url\n\nRequired headers:\nCookie: session_token is handled by Postman after Login.\n\nRequired body:\nNone\n\nExpected success response:\n{ status: \"ok\", data: { activeUserIds: [] }, message: \"Presence retrieved successfully\" }\n\nExpected error responses:\n401 unauthorized, 500 unexpected server error."
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('Status code is 200', () => pm.response.to.have.status(200));",
                  "pm.test('Response time is acceptable', () => pm.expect(pm.response.responseTime).to.be.below(2000));",
                  "pm.test('Response is JSON', () => pm.response.to.be.json);",
                  "pm.test('Content-Type is JSON', () => pm.expect(pm.response.headers.get('Content-Type') || '').to.include('application/json'));",
                  "pm.test('Response envelope is valid', () => {",
                  "  const body = pm.response.json();",
                  "  pm.expect(body).to.have.property('status');",
                  "  pm.expect(body).to.have.property('data');",
                  "  pm.expect(body).to.have.property('message');",
                  "});",
                  "pm.test('Returns activeUserIds array', () => {",
                  "  const body = pm.response.json();",
                  "  pm.expect(body.data.activeUserIds).to.be.an('array');",
                  "});"
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "Posts",
      "description": "Module 9 post create/read/like requests. Module 10 delete requests live in Module 10 Backend.",
      "item": [
        {
          "name": "Create Post",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{base_url}}/posts",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "posts"
              ]
            },
            "description": "Endpoint: POST /posts\n\nSetup instructions:\nRun Login first. user_id is taken from the session cookie.\n\nRequired variables:\n- base_url\n\nRequired headers:\nContent-Type: application/json\n\nRequired body:\nJSON with content.\n\nExpected success response:\n{ status: \"ok\", data: { post }, message: \"Post created successfully\" }\n\nExpected error responses:\n400 invalid post data, 401 unauthorized, 500 unexpected server error.",
            "body": {
              "mode": "raw",
              "raw": "{\n  \"content\": \"{{post_content}}\"\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('Status code is 201', () => pm.response.to.have.status(201));",
                  "pm.test('Response time is acceptable', () => pm.expect(pm.response.responseTime).to.be.below(2000));",
                  "pm.test('Response is JSON', () => pm.response.to.be.json);",
                  "pm.test('Content-Type is JSON', () => pm.expect(pm.response.headers.get('Content-Type') || '').to.include('application/json'));",
                  "pm.test('Response envelope is valid', () => {",
                  "  const body = pm.response.json();",
                  "  pm.expect(body).to.have.property('status');",
                  "  pm.expect(body).to.have.property('data');",
                  "  pm.expect(body).to.have.property('message');",
                  "});",
                  "pm.test('Captures post_id', () => {",
                  "  const body = pm.response.json();",
                  "  pm.expect(body.data.post._id).to.be.a('string');",
                  "  pm.environment.set('post_id', body.data.post._id);",
                  "});"
                ]
              }
            }
          ]
        },
        {
          "name": "Get All Posts",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{base_url}}/posts",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "posts"
              ]
            },
            "description": "Endpoint: GET /posts\n\nSetup instructions:\nOptional: run Create Post first so at least one post exists.\n\nRequired variables:\n- base_url\n\nRequired headers:\nNone\n\nRequired body:\nNone\n\nExpected success response:\n{ status: \"ok\", data: { posts: [] }, message: \"Posts retrieved successfully\" }\n\nExpected error responses:\n500 unexpected server error."
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('Status code is 200', () => pm.response.to.have.status(200));",
                  "pm.test('Response time is acceptable', () => pm.expect(pm.response.responseTime).to.be.below(2000));",
                  "pm.test('Response is JSON', () => pm.response.to.be.json);",
                  "pm.test('Content-Type is JSON', () => pm.expect(pm.response.headers.get('Content-Type') || '').to.include('application/json'));",
                  "pm.test('Response envelope is valid', () => {",
                  "  const body = pm.response.json();",
                  "  pm.expect(body).to.have.property('status');",
                  "  pm.expect(body).to.have.property('data');",
                  "  pm.expect(body).to.have.property('message');",
                  "});",
                  "pm.test('Returns posts array', () => {",
                  "  const body = pm.response.json();",
                  "  pm.expect(body.data.posts).to.be.an('array');",
                  "});"
                ]
              }
            }
          ]
        },
        {
          "name": "Update Post (likes)",
          "request": {
            "method": "PATCH",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{base_url}}/posts/{{post_id}}",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "posts",
                "{{post_id}}"
              ]
            },
            "description": "Endpoint: PATCH /posts/:id\n\nSetup instructions:\nRun Create Post first or set post_id to an existing post.\n\nRequired variables:\n- base_url\n- post_id\n\nRequired headers:\nContent-Type: application/json\n\nRequired body:\nJSON with likes increment, for example { \"likes\": 1 }.\n\nExpected success response:\n{ status: \"ok\", data: { post }, message: \"Post updated successfully\" }\n\nExpected error responses:\n400 invalid post data, 404 post not found, 500 unexpected server error.",
            "body": {
              "mode": "raw",
              "raw": "{\n  \"likes\": {{likes_delta}}\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('Status code is 200', () => pm.response.to.have.status(200));",
                  "pm.test('Response time is acceptable', () => pm.expect(pm.response.responseTime).to.be.below(2000));",
                  "pm.test('Response is JSON', () => pm.response.to.be.json);",
                  "pm.test('Content-Type is JSON', () => pm.expect(pm.response.headers.get('Content-Type') || '').to.include('application/json'));",
                  "pm.test('Response envelope is valid', () => {",
                  "  const body = pm.response.json();",
                  "  pm.expect(body).to.have.property('status');",
                  "  pm.expect(body).to.have.property('data');",
                  "  pm.expect(body).to.have.property('message');",
                  "});",
                  "pm.test('Message is Post updated successfully', () => {",
                  "  const body = pm.response.json();",
                  "  pm.expect(body.status).to.equal('ok');",
                  "  pm.expect(body.message).to.equal('Post updated successfully');",
                  "});"
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "Comments",
      "description": "Module 9 comment create/read/update requests. Module 10 delete requests live in Module 10 Backend.",
      "item": [
        {
          "name": "Create Comment",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{base_url}}/comments",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "comments"
              ]
            },
            "description": "Endpoint: POST /comments\n\nSetup instructions:\nRun Login and Create Post first. user_id is taken from session cookie.\n\nRequired variables:\n- base_url\n- post_id\n\nRequired headers:\nContent-Type: application/json\n\nRequired body:\nJSON with post_id and content.\n\nExpected success response:\n{ status: \"ok\", data: { comment }, message: \"Comment added successfully\" }\n\nExpected error responses:\n400 invalid comment data, 401 unauthorized, 404 post not found, 500 unexpected server error.",
            "body": {
              "mode": "raw",
              "raw": "{\n  \"post_id\": \"{{post_id}}\",\n  \"content\": \"{{comment_content}}\"\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('Status code is 201', () => pm.response.to.have.status(201));",
                  "pm.test('Response time is acceptable', () => pm.expect(pm.response.responseTime).to.be.below(2000));",
                  "pm.test('Response is JSON', () => pm.response.to.be.json);",
                  "pm.test('Content-Type is JSON', () => pm.expect(pm.response.headers.get('Content-Type') || '').to.include('application/json'));",
                  "pm.test('Response envelope is valid', () => {",
                  "  const body = pm.response.json();",
                  "  pm.expect(body).to.have.property('status');",
                  "  pm.expect(body).to.have.property('data');",
                  "  pm.expect(body).to.have.property('message');",
                  "});",
                  "pm.test('Captures comment_id', () => {",
                  "  const body = pm.response.json();",
                  "  pm.expect(body.data.comment._id).to.be.a('string');",
                  "  pm.environment.set('comment_id', body.data.comment._id);",
                  "});"
                ]
              }
            }
          ]
        },
        {
          "name": "Get All Comments",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{base_url}}/comments",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "comments"
              ]
            },
            "description": "Endpoint: GET /comments\n\nSetup instructions:\nOptional: run Create Comment first so at least one comment exists.\n\nRequired variables:\n- base_url\n\nRequired headers:\nNone\n\nRequired body:\nNone\n\nExpected success response:\n{ status: \"ok\", data: { comments: [] }, message: \"Comments retrieved successfully\" }\n\nExpected error responses:\n500 unexpected server error."
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('Status code is 200', () => pm.response.to.have.status(200));",
                  "pm.test('Response time is acceptable', () => pm.expect(pm.response.responseTime).to.be.below(2000));",
                  "pm.test('Response is JSON', () => pm.response.to.be.json);",
                  "pm.test('Content-Type is JSON', () => pm.expect(pm.response.headers.get('Content-Type') || '').to.include('application/json'));",
                  "pm.test('Response envelope is valid', () => {",
                  "  const body = pm.response.json();",
                  "  pm.expect(body).to.have.property('status');",
                  "  pm.expect(body).to.have.property('data');",
                  "  pm.expect(body).to.have.property('message');",
                  "});",
                  "pm.test('Returns comments array', () => {",
                  "  const body = pm.response.json();",
                  "  pm.expect(body.data.comments).to.be.an('array');",
                  "});"
                ]
              }
            }
          ]
        },
        {
          "name": "Update Comment",
          "request": {
            "method": "PATCH",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{base_url}}/comments/{{comment_id}}",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "comments",
                "{{comment_id}}"
              ]
            },
            "description": "Endpoint: PATCH /comments/:id\n\nSetup instructions:\nRun Create Comment first or set comment_id to an existing comment.\n\nRequired variables:\n- base_url\n- comment_id\n\nRequired headers:\nContent-Type: application/json\n\nRequired body:\nJSON with content and/or likes increment.\n\nExpected success response:\n{ status: \"ok\", data: { comment }, message: \"Comment updated successfully\" }\n\nExpected error responses:\n400 invalid comment data, 404 comment not found, 500 unexpected server error.",
            "body": {
              "mode": "raw",
              "raw": "{\n  \"content\": \"{{updated_comment_content}}\",\n  \"likes\": {{likes_delta}}\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('Status code is 200', () => pm.response.to.have.status(200));",
                  "pm.test('Response time is acceptable', () => pm.expect(pm.response.responseTime).to.be.below(2000));",
                  "pm.test('Response is JSON', () => pm.response.to.be.json);",
                  "pm.test('Content-Type is JSON', () => pm.expect(pm.response.headers.get('Content-Type') || '').to.include('application/json'));",
                  "pm.test('Response envelope is valid', () => {",
                  "  const body = pm.response.json();",
                  "  pm.expect(body).to.have.property('status');",
                  "  pm.expect(body).to.have.property('data');",
                  "  pm.expect(body).to.have.property('message');",
                  "});",
                  "pm.test('Message is Comment updated successfully', () => {",
                  "  const body = pm.response.json();",
                  "  pm.expect(body.status).to.equal('ok');",
                  "  pm.expect(body.message).to.equal('Comment updated successfully');",
                  "});"
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "Replies",
      "description": "Module 9 reply create/read/update requests.",
      "item": [
        {
          "name": "Create Reply",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{base_url}}/replies",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "replies"
              ]
            },
            "description": "Endpoint: POST /replies\n\nSetup instructions:\nRun Login, Create Post, and Create Comment first.\n\nRequired variables:\n- base_url\n- post_id\n- comment_id\n\nRequired headers:\nContent-Type: application/json\n\nRequired body:\nJSON with parent_id, parent_type, root_comment_id, post_id, content, and depth.\n\nExpected success response:\n{ status: \"ok\", data: { reply }, message: \"Reply added successfully\" }\n\nExpected error responses:\n400 invalid reply data, 401 unauthorized, 404 parent not found, 500 unexpected server error.",
            "body": {
              "mode": "raw",
              "raw": "{\n  \"parent_id\": \"{{comment_id}}\",\n  \"parent_type\": \"{{parent_type}}\",\n  \"root_comment_id\": \"{{comment_id}}\",\n  \"post_id\": \"{{post_id}}\",\n  \"content\": \"{{reply_content}}\",\n  \"depth\": {{reply_depth}}\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('Status code is 201', () => pm.response.to.have.status(201));",
                  "pm.test('Response time is acceptable', () => pm.expect(pm.response.responseTime).to.be.below(2000));",
                  "pm.test('Response is JSON', () => pm.response.to.be.json);",
                  "pm.test('Content-Type is JSON', () => pm.expect(pm.response.headers.get('Content-Type') || '').to.include('application/json'));",
                  "pm.test('Response envelope is valid', () => {",
                  "  const body = pm.response.json();",
                  "  pm.expect(body).to.have.property('status');",
                  "  pm.expect(body).to.have.property('data');",
                  "  pm.expect(body).to.have.property('message');",
                  "});",
                  "pm.test('Captures reply_id', () => {",
                  "  const body = pm.response.json();",
                  "  pm.expect(body.data.reply._id).to.be.a('string');",
                  "  pm.environment.set('reply_id', body.data.reply._id);",
                  "});"
                ]
              }
            }
          ]
        },
        {
          "name": "Get Replies by Post",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{base_url}}/replies?post_id={{post_id}}",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "replies"
              ],
              "query": [
                {
                  "key": "post_id",
                  "value": "{{post_id}}"
                }
              ]
            },
            "description": "Endpoint: GET /replies?post_id=:post_id\n\nSetup instructions:\nRun Create Post first. Query parameter is optional in backend, but this request filters by post.\n\nRequired variables:\n- base_url\n- post_id\n\nRequired headers:\nNone\n\nRequired body:\nNone\n\nExpected success response:\n{ status: \"ok\", data: { replies: [] }, message: \"Replies retrieved successfully\" }\n\nExpected error responses:\n400 invalid post_id, 500 unexpected server error."
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('Status code is 200', () => pm.response.to.have.status(200));",
                  "pm.test('Response time is acceptable', () => pm.expect(pm.response.responseTime).to.be.below(2000));",
                  "pm.test('Response is JSON', () => pm.response.to.be.json);",
                  "pm.test('Content-Type is JSON', () => pm.expect(pm.response.headers.get('Content-Type') || '').to.include('application/json'));",
                  "pm.test('Response envelope is valid', () => {",
                  "  const body = pm.response.json();",
                  "  pm.expect(body).to.have.property('status');",
                  "  pm.expect(body).to.have.property('data');",
                  "  pm.expect(body).to.have.property('message');",
                  "});",
                  "pm.test('Returns replies array', () => {",
                  "  const body = pm.response.json();",
                  "  pm.expect(body.data.replies).to.be.an('array');",
                  "});"
                ]
              }
            }
          ]
        },
        {
          "name": "Update Reply (likes)",
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{base_url}}/replies/{{reply_id}}",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "replies",
                "{{reply_id}}"
              ]
            },
            "description": "Endpoint: PUT /replies/:id\n\nSetup instructions:\nRun Create Reply first or set reply_id to an existing reply.\n\nRequired variables:\n- base_url\n- reply_id\n\nRequired headers:\nContent-Type: application/json\n\nRequired body:\nJSON with likes increment.\n\nExpected success response:\n{ status: \"ok\", data: { reply }, message: \"Reply updated successfully\" }\n\nExpected error responses:\n400 invalid reply data, 404 reply not found, 500 unexpected server error.",
            "body": {
              "mode": "raw",
              "raw": "{\n  \"likes\": {{likes_delta}}\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('Status code is 200', () => pm.response.to.have.status(200));",
                  "pm.test('Response time is acceptable', () => pm.expect(pm.response.responseTime).to.be.below(2000));",
                  "pm.test('Response is JSON', () => pm.response.to.be.json);",
                  "pm.test('Content-Type is JSON', () => pm.expect(pm.response.headers.get('Content-Type') || '').to.include('application/json'));",
                  "pm.test('Response envelope is valid', () => {",
                  "  const body = pm.response.json();",
                  "  pm.expect(body).to.have.property('status');",
                  "  pm.expect(body).to.have.property('data');",
                  "  pm.expect(body).to.have.property('message');",
                  "});",
                  "pm.test('Message is Reply updated successfully', () => {",
                  "  const body = pm.response.json();",
                  "  pm.expect(body.status).to.equal('ok');",
                  "  pm.expect(body.message).to.equal('Reply updated successfully');",
                  "});"
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "Profile Pic",
      "description": "Module 9 profile picture upload and fetch requests.",
      "item": [
        {
          "name": "Upload Profile Picture",
          "request": {
            "method": "POST",
            "header": [],
            "url": {
              "raw": "{{base_url}}/profile-pic",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "profile-pic"
              ]
            },
            "description": "Endpoint: POST /profile-pic\n\nSetup instructions:\nRun Login first, then choose a JPEG or PNG file in the form-data image field.\n\nRequired variables:\n- base_url\n\nRequired headers:\nmultipart/form-data generated by Postman\n\nRequired body:\nform-data file field named image.\n\nExpected success response:\n{ status: \"ok\", data: {}, message: \"Profile picture uploaded successfully.\" }\n\nExpected error responses:\n400 missing/invalid/too-large image, 401 unauthorized, 500 unexpected server error.",
            "body": {
              "mode": "formdata",
              "formdata": [
                {
                  "key": "image",
                  "type": "file",
                  "src": "",
                  "description": "Choose a local JPEG or PNG file before sending."
                }
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('Status code is 200', () => pm.response.to.have.status(200));",
                  "pm.test('Response time is acceptable', () => pm.expect(pm.response.responseTime).to.be.below(2000));",
                  "pm.test('Response is JSON', () => pm.response.to.be.json);",
                  "pm.test('Content-Type is JSON', () => pm.expect(pm.response.headers.get('Content-Type') || '').to.include('application/json'));",
                  "pm.test('Response envelope is valid', () => {",
                  "  const body = pm.response.json();",
                  "  pm.expect(body).to.have.property('status');",
                  "  pm.expect(body).to.have.property('data');",
                  "  pm.expect(body).to.have.property('message');",
                  "});",
                  "pm.test('Message is Profile picture uploaded successfully.', () => {",
                  "  const body = pm.response.json();",
                  "  pm.expect(body.status).to.equal('ok');",
                  "  pm.expect(body.message).to.equal('Profile picture uploaded successfully.');",
                  "});"
                ]
              }
            }
          ]
        },
        {
          "name": "Get Profile Picture",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{base_url}}/profile-pic/{{user_id}}",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "profile-pic",
                "{{user_id}}"
              ]
            },
            "description": "Endpoint: GET /profile-pic/:userId\n\nSetup instructions:\nRun Create User and Upload Profile Picture first, or set user_id to a user with a profile picture.\n\nRequired variables:\n- base_url\n- user_id\n\nRequired headers:\nNone\n\nRequired body:\nNone\n\nExpected success response:\nRaw image binary with image Content-Type.\n\nExpected error responses:\n404 profile picture not found, 500 unexpected server error."
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('Status code is 200', () => pm.response.to.have.status(200));",
                  "pm.test('Response time is acceptable', () => pm.expect(pm.response.responseTime).to.be.below(2000));",
                  "pm.test('Content-Type is an image', () => pm.expect(pm.response.headers.get('Content-Type') || '').to.match(/^image\\/(jpeg|png)/));",
                  "pm.test('Response body is not empty', () => pm.expect(pm.response.text()).to.not.equal(''));"
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "Module 10 Backend",
      "description": "Completed Module 10 backend endpoints only. Use throwaway test data for happy-path delete requests.",
      "item": [
        {
          "name": "User Endpoint",
          "item": [
            {
              "name": "PATCH /user/:id (Happy Path)",
              "request": {
                "method": "PATCH",
                "header": [
                  {
                    "key": "Content-Type",
                    "value": "application/json"
                  }
                ],
                "url": {
                  "raw": "{{base_url}}/user/{{user_id}}",
                  "host": [
                    "{{base_url}}"
                  ],
                  "path": [
                    "user",
                    "{{user_id}}"
                  ]
                },
                "description": "Endpoint: PATCH /user/:id\n\nSetup instructions:\nRun Create User first or set user_id to a throwaway existing user. This request updates allowed user fields only.\n\nRequired variables:\n- base_url\n- user_id\n\nRequired headers:\nContent-Type: application/json\n\nRequired body:\nJSON with any allowed fields: first_name, last_name, birthday, email, password, status, location, occupation, auth_level. Non-empty password values are hashed. Empty/missing password does not overwrite existing password.\n\nExpected success response:\n{ status: \"ok\", data: { user }, message: \"User updated successfully\" } and user does not include password.\n\nExpected error responses:\n400 invalid user id or validation error, 404 user not found, 409 duplicate email, 500 unexpected server error.",
                "body": {
                  "mode": "raw",
                  "raw": "{\n  \"first_name\": \"{{updated_first_name}}\",\n  \"last_name\": \"{{updated_last_name}}\",\n  \"location\": \"{{updated_location}}\",\n  \"occupation\": \"{{updated_occupation}}\",\n  \"status\": {{status}}\n}",
                  "options": {
                    "raw": {
                      "language": "json"
                    }
                  }
                }
              },
              "event": [
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Status code is 200', () => pm.response.to.have.status(200));",
                      "pm.test('Response time is acceptable', () => pm.expect(pm.response.responseTime).to.be.below(2000));",
                      "pm.test('Response is JSON', () => pm.response.to.be.json);",
                      "pm.test('Content-Type is JSON', () => pm.expect(pm.response.headers.get('Content-Type') || '').to.include('application/json'));",
                      "pm.test('Response envelope is valid', () => {",
                      "  const body = pm.response.json();",
                      "  pm.expect(body).to.have.property('status');",
                      "  pm.expect(body).to.have.property('data');",
                      "  pm.expect(body).to.have.property('message');",
                      "});",
                      "pm.test('Password is not returned', () => {",
                      "  const body = pm.response.json();",
                      "  pm.expect(body.status).to.equal('ok');",
                      "  pm.expect(body.message).to.equal('User updated successfully');",
                      "  pm.expect(body.data.user).to.not.have.property('password');",
                      "});"
                    ]
                  }
                }
              ]
            },
            {
              "name": "PATCH /user/:id (Malformed ID)",
              "request": {
                "method": "PATCH",
                "header": [
                  {
                    "key": "Content-Type",
                    "value": "application/json"
                  }
                ],
                "url": {
                  "raw": "{{base_url}}/user/{{malformed_id}}",
                  "host": [
                    "{{base_url}}"
                  ],
                  "path": [
                    "user",
                    "{{malformed_id}}"
                  ]
                },
                "description": "Endpoint: PATCH /user/:id\n\nSetup instructions:\nRun Create User first or set user_id to a throwaway existing user. This request updates allowed user fields only.\n\nRequired variables:\n- base_url\n- user_id\n\nRequired headers:\nContent-Type: application/json\n\nRequired body:\nJSON with any allowed fields: first_name, last_name, birthday, email, password, status, location, occupation, auth_level. Non-empty password values are hashed. Empty/missing password does not overwrite existing password.\n\nExpected success response:\n{ status: \"ok\", data: { user }, message: \"User updated successfully\" } and user does not include password.\n\nExpected error responses:\n400 invalid user id or validation error, 404 user not found, 409 duplicate email, 500 unexpected server error.\n\nScenario: malformed MongoDB id. Expected status: 400.",
                "body": {
                  "mode": "raw",
                  "raw": "{\n  \"first_name\": \"{{updated_first_name}}\"\n}",
                  "options": {
                    "raw": {
                      "language": "json"
                    }
                  }
                }
              },
              "event": [
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Status code is 400', () => pm.response.to.have.status(400));",
                      "pm.test('Response time is acceptable', () => pm.expect(pm.response.responseTime).to.be.below(2000));",
                      "pm.test('Response is JSON', () => pm.response.to.be.json);",
                      "pm.test('Content-Type is JSON', () => pm.expect(pm.response.headers.get('Content-Type') || '').to.include('application/json'));",
                      "pm.test('Error response envelope is valid', () => {",
                      "  const body = pm.response.json();",
                      "  pm.expect(body.status).to.equal('error');",
                      "  pm.expect(body).to.have.property('data');",
                      "  pm.expect(body).to.have.property('message').that.is.a('string').and.is.not.empty;",
                      "});",
                      "pm.test('Error message is Invalid user id', () => {",
                      "  const body = pm.response.json();",
                      "  pm.expect(body.status).to.equal('error');",
                      "  pm.expect(body.message).to.equal('Invalid user id');",
                      "});"
                    ]
                  }
                }
              ]
            },
            {
              "name": "PATCH /user/:id (Nonexistent ID)",
              "request": {
                "method": "PATCH",
                "header": [
                  {
                    "key": "Content-Type",
                    "value": "application/json"
                  }
                ],
                "url": {
                  "raw": "{{base_url}}/user/{{nonexistent_user_id}}",
                  "host": [
                    "{{base_url}}"
                  ],
                  "path": [
                    "user",
                    "{{nonexistent_user_id}}"
                  ]
                },
                "description": "Endpoint: PATCH /user/:id\n\nSetup instructions:\nRun Create User first or set user_id to a throwaway existing user. This request updates allowed user fields only.\n\nRequired variables:\n- base_url\n- user_id\n\nRequired headers:\nContent-Type: application/json\n\nRequired body:\nJSON with any allowed fields: first_name, last_name, birthday, email, password, status, location, occupation, auth_level. Non-empty password values are hashed. Empty/missing password does not overwrite existing password.\n\nExpected success response:\n{ status: \"ok\", data: { user }, message: \"User updated successfully\" } and user does not include password.\n\nExpected error responses:\n400 invalid user id or validation error, 404 user not found, 409 duplicate email, 500 unexpected server error.\n\nScenario: valid ObjectId format but no matching user. Expected status: 404.",
                "body": {
                  "mode": "raw",
                  "raw": "{\n  \"first_name\": \"{{updated_first_name}}\"\n}",
                  "options": {
                    "raw": {
                      "language": "json"
                    }
                  }
                }
              },
              "event": [
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Status code is 404', () => pm.response.to.have.status(404));",
                      "pm.test('Response time is acceptable', () => pm.expect(pm.response.responseTime).to.be.below(2000));",
                      "pm.test('Response is JSON', () => pm.response.to.be.json);",
                      "pm.test('Content-Type is JSON', () => pm.expect(pm.response.headers.get('Content-Type') || '').to.include('application/json'));",
                      "pm.test('Error response envelope is valid', () => {",
                      "  const body = pm.response.json();",
                      "  pm.expect(body.status).to.equal('error');",
                      "  pm.expect(body).to.have.property('data');",
                      "  pm.expect(body).to.have.property('message').that.is.a('string').and.is.not.empty;",
                      "});",
                      "pm.test('Error message is User not found', () => {",
                      "  const body = pm.response.json();",
                      "  pm.expect(body.status).to.equal('error');",
                      "  pm.expect(body.message).to.equal('User not found');",
                      "});"
                    ]
                  }
                }
              ]
            },
            {
              "name": "DELETE /user/:id (Happy Path)",
              "request": {
                "method": "DELETE",
                "header": [],
                "url": {
                  "raw": "{{base_url}}/user/{{user_id}}",
                  "host": [
                    "{{base_url}}"
                  ],
                  "path": [
                    "user",
                    "{{user_id}}"
                  ]
                },
                "description": "Endpoint: DELETE /user/:id\n\nSetup instructions:\nSet user_id to a throwaway user. This permanently deletes the user and related posts/comments/replies/sessions/profile picture records.\n\nRequired variables:\n- base_url\n- user_id\n\nRequired headers:\nNone\n\nRequired body:\nNone\n\nExpected success response:\n{ status: \"ok\", data: {}, message: \"User deleted successfully\" }\n\nExpected error responses:\n400 invalid user id, 404 user not found, 500 unexpected server error."
              },
              "event": [
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Status code is 200', () => pm.response.to.have.status(200));",
                      "pm.test('Response time is acceptable', () => pm.expect(pm.response.responseTime).to.be.below(2000));",
                      "pm.test('Response is JSON', () => pm.response.to.be.json);",
                      "pm.test('Content-Type is JSON', () => pm.expect(pm.response.headers.get('Content-Type') || '').to.include('application/json'));",
                      "pm.test('Response envelope is valid', () => {",
                      "  const body = pm.response.json();",
                      "  pm.expect(body).to.have.property('status');",
                      "  pm.expect(body).to.have.property('data');",
                      "  pm.expect(body).to.have.property('message');",
                      "});",
                      "pm.test('Message is User deleted successfully', () => {",
                      "  const body = pm.response.json();",
                      "  pm.expect(body.status).to.equal('ok');",
                      "  pm.expect(body.message).to.equal('User deleted successfully');",
                      "});"
                    ]
                  }
                }
              ]
            },
            {
              "name": "DELETE /user/:id (Malformed ID)",
              "request": {
                "method": "DELETE",
                "header": [],
                "url": {
                  "raw": "{{base_url}}/user/{{malformed_id}}",
                  "host": [
                    "{{base_url}}"
                  ],
                  "path": [
                    "user",
                    "{{malformed_id}}"
                  ]
                },
                "description": "Endpoint: DELETE /user/:id\n\nSetup instructions:\nSet user_id to a throwaway user. This permanently deletes the user and related posts/comments/replies/sessions/profile picture records.\n\nRequired variables:\n- base_url\n- user_id\n\nRequired headers:\nNone\n\nRequired body:\nNone\n\nExpected success response:\n{ status: \"ok\", data: {}, message: \"User deleted successfully\" }\n\nExpected error responses:\n400 invalid user id, 404 user not found, 500 unexpected server error.\n\nScenario: malformed MongoDB id. Expected status: 400."
              },
              "event": [
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Status code is 400', () => pm.response.to.have.status(400));",
                      "pm.test('Response time is acceptable', () => pm.expect(pm.response.responseTime).to.be.below(2000));",
                      "pm.test('Response is JSON', () => pm.response.to.be.json);",
                      "pm.test('Content-Type is JSON', () => pm.expect(pm.response.headers.get('Content-Type') || '').to.include('application/json'));",
                      "pm.test('Error response envelope is valid', () => {",
                      "  const body = pm.response.json();",
                      "  pm.expect(body.status).to.equal('error');",
                      "  pm.expect(body).to.have.property('data');",
                      "  pm.expect(body).to.have.property('message').that.is.a('string').and.is.not.empty;",
                      "});",
                      "pm.test('Error message is Invalid user id', () => {",
                      "  const body = pm.response.json();",
                      "  pm.expect(body.status).to.equal('error');",
                      "  pm.expect(body.message).to.equal('Invalid user id');",
                      "});"
                    ]
                  }
                }
              ]
            },
            {
              "name": "DELETE /user/:id (Nonexistent ID)",
              "request": {
                "method": "DELETE",
                "header": [],
                "url": {
                  "raw": "{{base_url}}/user/{{nonexistent_user_id}}",
                  "host": [
                    "{{base_url}}"
                  ],
                  "path": [
                    "user",
                    "{{nonexistent_user_id}}"
                  ]
                },
                "description": "Endpoint: DELETE /user/:id\n\nSetup instructions:\nSet user_id to a throwaway user. This permanently deletes the user and related posts/comments/replies/sessions/profile picture records.\n\nRequired variables:\n- base_url\n- user_id\n\nRequired headers:\nNone\n\nRequired body:\nNone\n\nExpected success response:\n{ status: \"ok\", data: {}, message: \"User deleted successfully\" }\n\nExpected error responses:\n400 invalid user id, 404 user not found, 500 unexpected server error.\n\nScenario: valid ObjectId format but no matching user. Expected status: 404."
              },
              "event": [
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Status code is 404', () => pm.response.to.have.status(404));",
                      "pm.test('Response time is acceptable', () => pm.expect(pm.response.responseTime).to.be.below(2000));",
                      "pm.test('Response is JSON', () => pm.response.to.be.json);",
                      "pm.test('Content-Type is JSON', () => pm.expect(pm.response.headers.get('Content-Type') || '').to.include('application/json'));",
                      "pm.test('Error response envelope is valid', () => {",
                      "  const body = pm.response.json();",
                      "  pm.expect(body.status).to.equal('error');",
                      "  pm.expect(body).to.have.property('data');",
                      "  pm.expect(body).to.have.property('message').that.is.a('string').and.is.not.empty;",
                      "});",
                      "pm.test('Error message is User not found', () => {",
                      "  const body = pm.response.json();",
                      "  pm.expect(body.status).to.equal('error');",
                      "  pm.expect(body.message).to.equal('User not found');",
                      "});"
                    ]
                  }
                }
              ]
            }
          ]
        },
        {
          "name": "Post Endpoint",
          "item": [
            {
              "name": "DELETE /posts/:id (Happy Path)",
              "request": {
                "method": "DELETE",
                "header": [],
                "url": {
                  "raw": "{{base_url}}/posts/{{post_id}}",
                  "host": [
                    "{{base_url}}"
                  ],
                  "path": [
                    "posts",
                    "{{post_id}}"
                  ]
                },
                "description": "Endpoint: DELETE /posts/:id\n\nSetup instructions:\nSet post_id to a throwaway post. This permanently deletes the post plus attached comments and related replies.\n\nRequired variables:\n- base_url\n- post_id\n\nRequired headers:\nNone\n\nRequired body:\nNone\n\nExpected success response:\n{ status: \"ok\", data: {}, message: \"Post deleted successfully\" }\n\nExpected error responses:\n400 invalid post id, 404 post not found, 500 unexpected server error."
              },
              "event": [
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Status code is 200', () => pm.response.to.have.status(200));",
                      "pm.test('Response time is acceptable', () => pm.expect(pm.response.responseTime).to.be.below(2000));",
                      "pm.test('Response is JSON', () => pm.response.to.be.json);",
                      "pm.test('Content-Type is JSON', () => pm.expect(pm.response.headers.get('Content-Type') || '').to.include('application/json'));",
                      "pm.test('Response envelope is valid', () => {",
                      "  const body = pm.response.json();",
                      "  pm.expect(body).to.have.property('status');",
                      "  pm.expect(body).to.have.property('data');",
                      "  pm.expect(body).to.have.property('message');",
                      "});",
                      "pm.test('Message is Post deleted successfully', () => {",
                      "  const body = pm.response.json();",
                      "  pm.expect(body.status).to.equal('ok');",
                      "  pm.expect(body.message).to.equal('Post deleted successfully');",
                      "});"
                    ]
                  }
                }
              ]
            },
            {
              "name": "DELETE /posts/:id (Malformed ID)",
              "request": {
                "method": "DELETE",
                "header": [],
                "url": {
                  "raw": "{{base_url}}/posts/{{malformed_id}}",
                  "host": [
                    "{{base_url}}"
                  ],
                  "path": [
                    "posts",
                    "{{malformed_id}}"
                  ]
                },
                "description": "Endpoint: DELETE /posts/:id\n\nSetup instructions:\nSet post_id to a throwaway post. This permanently deletes the post plus attached comments and related replies.\n\nRequired variables:\n- base_url\n- post_id\n\nRequired headers:\nNone\n\nRequired body:\nNone\n\nExpected success response:\n{ status: \"ok\", data: {}, message: \"Post deleted successfully\" }\n\nExpected error responses:\n400 invalid post id, 404 post not found, 500 unexpected server error.\n\nScenario: malformed MongoDB id. Expected status: 400."
              },
              "event": [
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Status code is 400', () => pm.response.to.have.status(400));",
                      "pm.test('Response time is acceptable', () => pm.expect(pm.response.responseTime).to.be.below(2000));",
                      "pm.test('Response is JSON', () => pm.response.to.be.json);",
                      "pm.test('Content-Type is JSON', () => pm.expect(pm.response.headers.get('Content-Type') || '').to.include('application/json'));",
                      "pm.test('Error response envelope is valid', () => {",
                      "  const body = pm.response.json();",
                      "  pm.expect(body.status).to.equal('error');",
                      "  pm.expect(body).to.have.property('data');",
                      "  pm.expect(body).to.have.property('message').that.is.a('string').and.is.not.empty;",
                      "});",
                      "pm.test('Error message is Invalid post id', () => {",
                      "  const body = pm.response.json();",
                      "  pm.expect(body.status).to.equal('error');",
                      "  pm.expect(body.message).to.equal('Invalid post id');",
                      "});"
                    ]
                  }
                }
              ]
            },
            {
              "name": "DELETE /posts/:id (Nonexistent ID)",
              "request": {
                "method": "DELETE",
                "header": [],
                "url": {
                  "raw": "{{base_url}}/posts/{{nonexistent_post_id}}",
                  "host": [
                    "{{base_url}}"
                  ],
                  "path": [
                    "posts",
                    "{{nonexistent_post_id}}"
                  ]
                },
                "description": "Endpoint: DELETE /posts/:id\n\nSetup instructions:\nSet post_id to a throwaway post. This permanently deletes the post plus attached comments and related replies.\n\nRequired variables:\n- base_url\n- post_id\n\nRequired headers:\nNone\n\nRequired body:\nNone\n\nExpected success response:\n{ status: \"ok\", data: {}, message: \"Post deleted successfully\" }\n\nExpected error responses:\n400 invalid post id, 404 post not found, 500 unexpected server error.\n\nScenario: valid ObjectId format but no matching post. Expected status: 404."
              },
              "event": [
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Status code is 404', () => pm.response.to.have.status(404));",
                      "pm.test('Response time is acceptable', () => pm.expect(pm.response.responseTime).to.be.below(2000));",
                      "pm.test('Response is JSON', () => pm.response.to.be.json);",
                      "pm.test('Content-Type is JSON', () => pm.expect(pm.response.headers.get('Content-Type') || '').to.include('application/json'));",
                      "pm.test('Error response envelope is valid', () => {",
                      "  const body = pm.response.json();",
                      "  pm.expect(body.status).to.equal('error');",
                      "  pm.expect(body).to.have.property('data');",
                      "  pm.expect(body).to.have.property('message').that.is.a('string').and.is.not.empty;",
                      "});",
                      "pm.test('Error message is Post not found', () => {",
                      "  const body = pm.response.json();",
                      "  pm.expect(body.status).to.equal('error');",
                      "  pm.expect(body.message).to.equal('Post not found');",
                      "});"
                    ]
                  }
                }
              ]
            }
          ]
        },
        {
          "name": "Comment Endpoint",
          "item": [
            {
              "name": "DELETE /comments/:id (Happy Path)",
              "request": {
                "method": "DELETE",
                "header": [],
                "url": {
                  "raw": "{{base_url}}/comments/{{comment_id}}",
                  "host": [
                    "{{base_url}}"
                  ],
                  "path": [
                    "comments",
                    "{{comment_id}}"
                  ]
                },
                "description": "Endpoint: DELETE /comments/:id\n\nSetup instructions:\nSet comment_id to a throwaway comment. This permanently deletes the comment, related replies, and removes the comment id from the parent post comments array.\n\nRequired variables:\n- base_url\n- comment_id\n\nRequired headers:\nNone\n\nRequired body:\nNone\n\nExpected success response:\n{ status: \"ok\", data: {}, message: \"Comment deleted successfully\" }\n\nExpected error responses:\n400 invalid comment id, 404 comment not found, 500 unexpected server error."
              },
              "event": [
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Status code is 200', () => pm.response.to.have.status(200));",
                      "pm.test('Response time is acceptable', () => pm.expect(pm.response.responseTime).to.be.below(2000));",
                      "pm.test('Response is JSON', () => pm.response.to.be.json);",
                      "pm.test('Content-Type is JSON', () => pm.expect(pm.response.headers.get('Content-Type') || '').to.include('application/json'));",
                      "pm.test('Response envelope is valid', () => {",
                      "  const body = pm.response.json();",
                      "  pm.expect(body).to.have.property('status');",
                      "  pm.expect(body).to.have.property('data');",
                      "  pm.expect(body).to.have.property('message');",
                      "});",
                      "pm.test('Message is Comment deleted successfully', () => {",
                      "  const body = pm.response.json();",
                      "  pm.expect(body.status).to.equal('ok');",
                      "  pm.expect(body.message).to.equal('Comment deleted successfully');",
                      "});"
                    ]
                  }
                }
              ]
            },
            {
              "name": "DELETE /comments/:id (Malformed ID)",
              "request": {
                "method": "DELETE",
                "header": [],
                "url": {
                  "raw": "{{base_url}}/comments/{{malformed_id}}",
                  "host": [
                    "{{base_url}}"
                  ],
                  "path": [
                    "comments",
                    "{{malformed_id}}"
                  ]
                },
                "description": "Endpoint: DELETE /comments/:id\n\nSetup instructions:\nSet comment_id to a throwaway comment. This permanently deletes the comment, related replies, and removes the comment id from the parent post comments array.\n\nRequired variables:\n- base_url\n- comment_id\n\nRequired headers:\nNone\n\nRequired body:\nNone\n\nExpected success response:\n{ status: \"ok\", data: {}, message: \"Comment deleted successfully\" }\n\nExpected error responses:\n400 invalid comment id, 404 comment not found, 500 unexpected server error.\n\nScenario: malformed MongoDB id. Expected status: 400."
              },
              "event": [
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Status code is 400', () => pm.response.to.have.status(400));",
                      "pm.test('Response time is acceptable', () => pm.expect(pm.response.responseTime).to.be.below(2000));",
                      "pm.test('Response is JSON', () => pm.response.to.be.json);",
                      "pm.test('Content-Type is JSON', () => pm.expect(pm.response.headers.get('Content-Type') || '').to.include('application/json'));",
                      "pm.test('Error response envelope is valid', () => {",
                      "  const body = pm.response.json();",
                      "  pm.expect(body.status).to.equal('error');",
                      "  pm.expect(body).to.have.property('data');",
                      "  pm.expect(body).to.have.property('message').that.is.a('string').and.is.not.empty;",
                      "});",
                      "pm.test('Error message is Invalid comment id', () => {",
                      "  const body = pm.response.json();",
                      "  pm.expect(body.status).to.equal('error');",
                      "  pm.expect(body.message).to.equal('Invalid comment id');",
                      "});"
                    ]
                  }
                }
              ]
            },
            {
              "name": "DELETE /comments/:id (Nonexistent ID)",
              "request": {
                "method": "DELETE",
                "header": [],
                "url": {
                  "raw": "{{base_url}}/comments/{{nonexistent_comment_id}}",
                  "host": [
                    "{{base_url}}"
                  ],
                  "path": [
                    "comments",
                    "{{nonexistent_comment_id}}"
                  ]
                },
                "description": "Endpoint: DELETE /comments/:id\n\nSetup instructions:\nSet comment_id to a throwaway comment. This permanently deletes the comment, related replies, and removes the comment id from the parent post comments array.\n\nRequired variables:\n- base_url\n- comment_id\n\nRequired headers:\nNone\n\nRequired body:\nNone\n\nExpected success response:\n{ status: \"ok\", data: {}, message: \"Comment deleted successfully\" }\n\nExpected error responses:\n400 invalid comment id, 404 comment not found, 500 unexpected server error.\n\nScenario: valid ObjectId format but no matching comment. Expected status: 404."
              },
              "event": [
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "pm.test('Status code is 404', () => pm.response.to.have.status(404));",
                      "pm.test('Response time is acceptable', () => pm.expect(pm.response.responseTime).to.be.below(2000));",
                      "pm.test('Response is JSON', () => pm.response.to.be.json);",
                      "pm.test('Content-Type is JSON', () => pm.expect(pm.response.headers.get('Content-Type') || '').to.include('application/json'));",
                      "pm.test('Error response envelope is valid', () => {",
                      "  const body = pm.response.json();",
                      "  pm.expect(body.status).to.equal('error');",
                      "  pm.expect(body).to.have.property('data');",
                      "  pm.expect(body).to.have.property('message').that.is.a('string').and.is.not.empty;",
                      "});",
                      "pm.test('Error message is Comment not found', () => {",
                      "  const body = pm.response.json();",
                      "  pm.expect(body.status).to.equal('error');",
                      "  pm.expect(body.message).to.equal('Comment not found');",
                      "});"
                    ]
                  }
                }
              ]
            }
          ]
        }
      ]
    }
  ]
}
